# Model Performance Report

**Generated**: 2025-11-27 21:43:37
**Platform**: linux
**Python**: 3.10.12
**CPU**: 6 cores (6 logical)
**RAM**: 7.44 GB

---

## Executive Summary

This report aggregates performance metrics from all AI models deployed in the     Local AI Robot Assistant project.

## Vision Models

### YOLOv11n Object Detection

**Status**: ✅ Success

```
Average FPS: 39.25
```

**Details**: See `scripts/test_yolo.py` for full benchmark.

### Depth Anything V2 Small

**Status**: ✅ Success

```
FPS: 30.0 | Latency: 33.32 ms
```

**Details**: See `scripts/test_depth.py` for full benchmark.

---

## Cognitive Core

### Moondream VLM (via Ollama)

**Status**: ✅ Success


**Details**: See `scripts/test_ollama_moondream.py` for full benchmark.

---

## Audio Models

### Wake Word Detection + Speech Recognition

**Status**: ✅ Success

**Note**: See test_audio_models.py output for details

**Details**: See `scripts/test_audio_models.py` for full benchmark.

### Piper TTS

**Status**: ✅ Success

**Note**: ~0.03s/word synthesis speed

**Details**: See `scripts/testing/audio/test_piper_tts.py` for full benchmark.

---

## Memory Budget Summary

Based on benchmark results:

| Component | Memory Usage | Notes |
|-----------|--------------|-------|
| OS + ROS2 | ~1.5 GB | Base system |
| YOLO (TensorRT) | ~100-200 MB | Real-time detection |
| Depth (TensorRT) | ~200-300 MB | Real-time depth |
| Moondream (Ollama) | ~3.0 GB | VLM inference |
| Whisper (faster-whisper) | ~700 MB | Speech recognition |
| Piper TTS | ~200 MB | Text-to-speech |
| **Total (Peak)** | **~5.5-6.0 GB** | Within 8GB limit |

---

## Recommendations

1. **YOLO + Depth**: Keep running continuously for reactive navigation (20+ FPS)
2. **Moondream**: Use on-demand for high-level reasoning (~0.5 FPS)
3. **Audio Pipeline**: Optimize Whisper memory usage or use TensorRT conversion
4. **Memory Management**: Monitor swap usage during simultaneous model operation

---

## Next Steps

- [ ] Profile combined YOLO + Depth + cognitive core operation
- [ ] Test memory pressure with all models loaded
- [ ] Optimize Whisper memory footprint
- [ ] Benchmark end-to-end latency (wake word → action)

---

*Report generated by `scripts/benchmarking/generate_performance_report.py`*

---

# JetPack 6.2 / power-profile measurements (Plan.md Phase 1)

**Device**: Jetson Orin Nano 8GB Super (L4T 36.4.7) · **Date**: 2026-09-24

## Baseline — 15W profile (`nvpmodel -m 0`, pre-change)

| Model | Metric | 15W result |
|-------|--------|------------|
| YOLOv11n (TensorRT FP16) | Average / median FPS | 41.90 / 41.11 |
| Depth Anything V2 Small (TensorRT) | FPS / latency | 28.1 / 35.63 ms |
| Moondream (Ollama, HTTP) | Vision encode / generation / total | 38 ms / 30.4 tok/s / 2.16 s |
| Moondream (Ollama, HTTP) | Peak `ollama` RSS | 2965.8 MB |

> MAXN SUPER (`nvpmodel -m 2` + `jetson_clocks`) results are recorded below —
> old numbers are retained for regression visibility, not overwritten.

## MAXN SUPER (`nvpmodel -m 2` + `jetson_clocks`) — after

**Date**: 2026-09-24 · CPU locked 1.728 GHz, GPU 1.02 GHz, EMC 3.199 GHz

| Model | Metric | 15W | MAXN SUPER | Δ |
|-------|--------|-----|------------|---|
| YOLOv11n (TensorRT FP16) | Average / median FPS | 41.90 / 41.11 | **98.21 / 98.53** | 2.34× |
| Depth Anything V2 Small (TensorRT) | FPS / latency | 28.1 / 35.63 ms | **55.1 / 18.15 ms** | 1.96× |
| Moondream (Ollama) | Vision encode | 38 ms | **24 ms** | 1.58× |
| Moondream (Ollama) | Generation | 30.4 tok/s | **49.0 tok/s** | 1.61× |
| Moondream (Ollama) | Total response | 2.16 s | **1.32 s** | 1.64× |
| Moondream (Ollama) | Peak RSS | 2965.8 MB | 2966.2 MB | — |
| Thermal (105 s sustained YOLO) | max tj | — | **53.5 °C** | (< 80 °C target) |

**Gate**: ✅ YOLO/Depth FPS did not regress (both improved), VLM tok/s improved (30.4 → 49.0). Rollback not needed — thermals well within budget.

## MAXN SUPER — Phase 2 llama.cpp re-check

_Pending: the `llamacpp` backend is not promoted, so no MAXN SUPER re-benchmark was run. If it is promoted later, re-run `scripts/testing/llm/test_llamacpp_moondream.py`._

---

# Cognitive Core — Phase 2: in-process llama.cpp (flag-gated)

**Date**: 2026-09-24 · `llama-cpp-python 0.3.35`, CUDA 12.6, `GGML_CUDA=on`,
`CMAKE_CUDA_ARCHITECTURES=87`, `n_gpu_layers=-1` (all 24 layers on CUDA0),
`n_ctx=2048`. Same weights as Ollama (`moondream:latest` Q4_0 blob), so only the
runtime differs.

| Metric | Ollama (HTTP) | llama.cpp (in-process) |
|--------|---------------|------------------------|
| Text/text-only generation | 30.4 tok/s | 28.8 tok/s |
| Vision end-to-end latency | ~2.16 s | ~3.79 s |
| Vision tok/s (incl. clip encode) | ~30 tok/s* | 14.5 tok/s |
| Peak RSS (host `ollama` proc vs test proc) | 2965.8 MB | 2856.2 MB |
| GPU offload | `size_vram == size` (100%) | all layers → CUDA0 |
| 5-min soak (153 queries) | — | 0 errors, RSS flat after warmup |

*Ollama reports `eval_duration` separately from vision encode; its 30.4 tok/s is
generation-only. The llama.cpp "vision tok/s" here divides tokens by the **whole**
call (including clip image encoding, ~1.5 s), which is why it looks lower.

**Conclusion**: the in-process path matches Ollama on pure generation speed,
sheds ~110 MB of peak RSS, and is stable over a 5-minute soak. However, its
vision encoder is not GPU-accelerated as effectively as Ollama's and roughly
doubles vision end-to-end latency. Per Plan.md ground rule #3 (flag-and-prove),
the default remains `ollama`; the `llamacpp` backend stays available behind
`cognitive_backend:=llamacpp` pending vision-encoder tuning (e.g. a GGUF with
`tokenizer.chat_template` for `MTMDChatHandler` GPU vision).
