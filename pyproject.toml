[project]
name = "local-ai-robot-assistant"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
    "numpy",
    "opencv-python",
    "torch",
    "transformers",
    "pyaudio",
    "pyserial",
]

[project.optional-dependencies]
dev = ["pytest", "black", "ruff"]
