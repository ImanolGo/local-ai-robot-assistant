# Placeholder CMakeLists.txt for perception_nodes package
cmake_minimum_required(VERSION 3.5)
project(perception_nodes)

# Find dependencies
find_package(ament_cmake REQUIRED)

# Package
ament_package()
